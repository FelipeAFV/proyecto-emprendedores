import { CheckAuthDirective } from './check-auth.directive';

describe('CheckAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckAuthDirective();
    expect(directive).toBeTruthy();
  });
});
